import * as React from 'react';

// Because at the moment of writing this there is no createContext in @types/react
// @ts-ignore
const MoleculeContext = React.createContext();

export default MoleculeContext;
